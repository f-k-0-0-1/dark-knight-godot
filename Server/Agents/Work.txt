Problems:
	1. Only in server player can see the joined player join player can't see
	2. Server player or joined player can't kill any enimies their health remain full 
	3. If server play dies the server player game crash with error: (online only offlie everything fine)
			E 0:02:14:848   _update_remote_player: Trying to assign invalid previously freed instance.
  			<GDScript Source>sync_node.gd:355 @ _update_remote_player()
 			 <Stack Trace> sync_node.gd:355 @ _update_remote_player()
				sync_node.gd:303 @ _on_packet_received()
				sync_node.gd:380 @ _process()
	4. Even when you are playing localy no player can kill any enemies
	5. Also Before making new server clear all things on both or kill and smae for existing the game even if player don't press server -e for forget
